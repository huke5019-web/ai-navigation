import type{MetadataRoute}from"next";import{prisma}from"@/lib/prisma";export const dynamic="force-dynamic";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL??"http://localhost:3000";const tools=await prisma.tool.findMany({where:{isActive:true,category:{isActive:true}},select:{slug:true,updatedAt:true}});return[{url:base,lastModified:new Date(),priority:1},...tools.map(t=>({url:`${base}/tools/${t.slug}`,lastModified:t.updatedAt,priority:.8}))]}
